# Runtime dependencies — everyone installs these.
numpy
scipy
torch
matplotlib
scikit-learn
tensorboard
pyyaml
pytest
h5py          # reading RadChar and RadioML (.h5) — P1 and P2 both need this
gradio        # scripts/inference_ui.py

# Tools, not dependencies — install only if you need them:
#   pip install kaggle       (P1/P2, for downloading the datasets)
#   pip install jupyterlab   (exploring datasets in a notebook)
onnx          # scripts/export_onnx.py and the Model page's retraining (scripts/retrain_from_feedback.py)
onnxruntime   # verifies every exported model against PyTorch
onnxscript    # torch's ONNX exporter needs it
